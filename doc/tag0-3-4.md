以下是用中文写的功能介绍，侧重「用户不易察觉但体验明显提升」的细节。常见功能（终端、Git、文件浏览、搜索）一笔带过。

---

# Vibe IDE 功能介绍

## 一句话定位

**为 AI Agent 与人协作而生的桌面终端 IDE。** 你在左侧管理多个 AI 会话，中间看终端输出 / 编辑文件，右侧操作 Git、辅助命令、文件树和搜索。三栏一体，不用来回切窗口。

---

## 你可能没注意到的体验细节

### 一、终端不只是终端

**1. 智能文件路径识别与跳转**

终端输出中任何文件路径——Windows 绝对路径 `E:\src\foo.ts`、Unix 绝对路径 `/home/user/foo.py`、相对路径 `src/bar.ts`、带引号路径 `"C:\path with spaces\file.txt"`——都会被自动识别并渲染为可点击链接。支持 `:行号` 和 `:行:列` 后缀，点击直接跳转到对应位置。

更进一步的：如果直接路径读不到文件，系统会判断是否为裸文件名（无目录分隔符），然后**递归搜索工作目录子目录**。搜索到唯一匹配则自动打开，多个匹配则静默忽略——你不会看到报错弹窗。

**2. 选中即跳转**

在终端中**选中一段文本，松开鼠标**，如果选中的文字恰好是一个文件路径，自动跳转打开。不需要右键菜单，不需要精确点击下划线链接。

**3. Agent 状态感知**

系统通过终端输出频率自动判断 AI Agent 是否在运行：
- 连续 300ms 有输出 → 标记为「运行中」
- 持续 2 秒无输出 → 标记为「空闲」

运行中的非活跃会话会在左侧列表显示 `>>` 跑马灯动画 + 边框脉冲——你切到别的 session 干活，Agent 跑完了你一眼就能看到。

**4. OSC 标题自动重命名**

当 AI Agent（如 Claude Code）通过 OSC 转义序列设置终端标题时，Vibe IDE 会自动将会话名更新为标题内容。同时过滤掉盲文 Spinner 字符（`⠐⠴⠤` 等）、Shell 进程名、路径类标题——你看到的是干净的会话名，不是乱码。

**但一旦你手动改过名字，自动改名就永久停用了**——尊重你的命名权。

**5. 翻页键智能路由**

PageUp/PageDown 的行为取决于终端状态：
- 内容超过一屏（有滚动条）→ 滚动终端视口
- 内容不足一屏 → **透传给 Shell**，不拦截

这意味着你在 `less`、`git log` 等分页器中翻页正常，在普通 Shell 中也正常——不会出现「快捷键吞掉了应该给 Shell 的按键」的问题。

**6. 聚焦感知光标闪烁**

终端获得焦点时光标闪烁，失去焦点时自动关闭闪烁。减少空闲时的渲染开销。

**7. 右键智能菜单**

- 有选中文本 → 右键 = 复制
- 无选中文本 → 右键 = 粘贴

一个右键搞定两种操作，不用记组合键。

**8. 终端保持挂载不销毁**

切 Session 或切到 Diff 视图时，终端组件**不被卸载**（用的是 CSS `display:none`）。切回来的时候不会重新初始化、不会丢失输出历史、不会重新连接 PTY。

---

### 二、会话管理的细节

**1. 克隆会话插入在父项下方**

右键克隆一个会话，新会话不是追加到列表末尾，而是**紧挨着原会话下方插入**。同时开多个 AI Agent 处理不同任务时，相关会话天然聚在一起。

**2. 最近目录快捷入口**

右键左侧面板空白区域，直接显示最近打开过的 10 个目录，一键创建新终端。不用每次去翻文件选择器。

**3. 悬停预览命令历史**

鼠标停在某个会话上 600ms，弹出该会话最近 30 条命令历史浮窗。每条命令旁有复制按钮。不用切换过去就能回顾这个 Agent 刚才干了什么。

**4. 会话 Emoji 自定义**

每个会话的图标不是固定的——你可以在设置菜单里自定义 Emoji 列表（一行一个），会话图标会根据 ID hash 从你的列表中选取。23 个默认 Emoji 覆盖了常见的场景区分。

**5. 命令历史去重**

连续输入相同命令不会重复记录。历史列表始终干净。

---

### 三、Git 面板的细节

**1. 超大仓库自动折叠**

当 untracked / unstaged / staged 文件数超过 500 时，对应区域**自动折叠**。避免打开 `node_modules` 爆炸的仓库时 UI 卡死。折叠只触发一次，后续你可以手动展开。

**2. 文件变更通知冷却机制**

监听文件系统变更时采用 **2 秒冷却期**：2 秒内的多次文件变更合并为一次通知。避免保存文件时触发十几次 Git 刷新。冷却期内如果持续有变更，会调度一次延迟通知，确保最后的状态不丢。

**3. 防 Stale 数据模式**

异步加载 Git 状态或 CLAUDE.md 命令时，`await` 返回后会先检查「加载时的路径是否还是当前路径」。如果用户已经在等待期间切到了另一个 Session，旧数据直接丢弃——不会出现 A 会话显示 B 会话数据的问题。

**4. 冲突标记检测**

在 staged diff 中自动扫描 `<<<<<<<` 冲突标记，帮你快速定位合并冲突。

**5. 错误 5 秒自动消失**

Git 操作的错误提示不会一直挂在那——5 秒后自动消失，不需要手动点关闭。

---

### 四、编辑器的细节

**1. 编码智能检测**

打开文件时自动检测 BOM（UTF-8/16/32 LE/BE）和 jschardet 推断编码，检测结果显示在标题栏（如 `BOM UTF-8` 或 `95%` 置信度）。二进制文件会被拒绝打开而不是显示乱码。

右键标题栏可以**换编码重新打开**或**换编码保存**，支持 GBK、Shift-JIS、EUC-KR、ISO-8859 系列、Big5 等 30+ 种编码。

**2. 防误判「已修改」**

编辑器加载内容后会设一个 `justLoaded` 标记，Monaco 初始化时触发的 `onDidChangeModelContent` 事件不会被误判为「用户修改了文件」。只有加载完成后你真正编辑了内容，才会显示 `● 未保存`。

**3. 95 种语言语法高亮**

不仅仅是常见的 TS/JS/Python/Rust/Go，还覆盖了 `.mjs`/`.cjs`/`.mts`/`.cts`、GraphQL、Dart、Lua、R、Scala、Clojure、F#、Julia、Elixir、Perl、CoffeeScript、Solidity、Protobuf、Handlebars/Pug/Twig/Razor/MDX、SystemVerilog/Verilog 等。项目里编辑任何文件几乎都有语法支持。

**4. 行号跳转边界保护**

从终端点击 `file.ts:99999` 跳转到远超文件行数的行号时，自动钳制到文件最后一行，不会报错或跳转到空白位置。

**5. 翻页联动**

在 Git 面板选中文件查看 diff 后，用 PageUp/PageDown 可以滚动编辑器内容。Git 面板的状态变化通过 `scrollTrigger` 传递到 DiffViewer，每次滚动半页高度。

---

### 五、启动与性能

**1. Windows COM 文件对话框预热**

应用启动 3 秒后，后台通过 PowerShell 预加载 `System.Windows.Forms` 程序集。Windows 的 COM 文件打开对话框（IFileOpenDialog）首次调用时会加载 OneDrive、Dropbox 等 Shell 扩展，冷启动耗时 1-5 秒。预热后用户点击「打开目录」时对话框秒开。

**2. GPU 强制硬件加速**

通过 Chromium 启动参数强制 ANGLE D3D11 渲染 + 禁用软件光栅化回退。终端 WebGL 渲染器走硬件加速，流式输出时不会 CPU 满载。如果 GPU 确实不可用，WebGL 上下文创建失败后自动回退到 DOM 渲染器——既追求极致性能，也有兜底。

**3. 单实例锁按路径区分**

不同路径的 exe 各自独立运行（通过 exe 路径 MD5 hash）。同一路径的 exe 是单实例，第二个窗口打开时会将路径参数转发给第一个窗口，自动创建 Session 或打开文件。

**4. 字体缩放绕过 Chromium 限制**

Chromium 会吃掉 `Ctrl+=` / `Ctrl+-` 做页面缩放。Vibe IDE 在主进程拦截这些按键，阻止默认缩放行为，通过 IPC 转发到渲染进程执行终端/编辑器的字体大小调节。终端和编辑器**各自独立记住字体大小**，切换视图时各自生效。

**5. TerminalView 懒加载**

终端组件使用 `React.lazy` + `Suspense` 动态导入，首屏不阻塞。

---

### 六、右侧面板

**1. 标签页可拖拽重排、可隐藏**

四个标签页（Git / Aux / File / Search）的**顺序和可见性**都持久化到 localStorage。右键标签页可以隐藏不需要的，至少保留一个。Ctrl+F 触发搜索时即使 Search 标签被隐藏也会自动显示。

**2. Ctrl+左右箭头切标签页**

切标签页时自动聚焦对应内容——Search 聚焦输入框，其他聚焦容器。键盘导航索引在切标签或切会话时自动复位。

**3. 面板折叠联动窗口大小**

折叠/展开右侧面板时，窗口宽度同步调整（面板宽度 + 分隔条宽度）。不是简单的 CSS 隐藏，而是真的把窗口缩窄了——最大化利用屏幕空间。

---

### 七、快捷键系统

**1. 完全可自定义**

所有快捷键（搜索、切终端、字体缩放、切标签页、翻页、命令历史等）都可以在「Keyboard Shortcuts」设置面板中重新绑定。点击录制新快捷键，支持 reset 恢复默认。

**2. 捕获阶段拦截**

全局快捷键在 DOM 事件的 **capture 阶段** 处理，在 xterm.js 之前拦截。这保证了 `Ctrl+F` 不会把 `F` 输入到终端，`Ctrl+ArrowUp` 不会被 Shell 吃掉。

**3. 历史弹窗全键盘操作**

`Ctrl+H` 弹出命令历史，ArrowUp/Down 选择，Enter 执行或点击发送，Escape 关闭。选中的条目自动 `scrollIntoView`，不会滚出视野。

---

### 八、搜索

**1. Ripgrep 优先，Node.js 兜底**

系统装了 `rg` 就用 ripgrep（JSON 输出解析，15 秒超时），没装就回退到 Node.js 递归搜索。结果最多 200 条，1MB 以上的文件自动跳过。

**2. 300ms 防抖**

输入即搜，但打字快时不会触发多次搜索——每次按键重置 300ms 计时器。

---

总结：Vibe IDE 不是一个大而全的 IDE，它聚焦于一件事——**让你在同一个窗口里高效地管理多个 AI Agent 会话**。每个细节都在减少操作摩擦，让你专注于与 AI 的协作本身，而不是工具的切换和等待。

好的，以下是 Git Worktree 功能的完整介绍，补到之前的功能介绍里：

---

### 九、Git Worktree：多分支并行开发

这是一个容易被当成"普通分支列表"而错过的重磅功能。

**背景**：`git worktree` 允许同一个仓库同时 checkout 多个分支到不同目录，互不干扰。典型的 AI 协作场景是：你在 `main` 分支干活，AI Agent 在另一个 worktree 分支上改代码——两个进程各自操作不同目录，不会相互踩文件。

Vibe IDE 把这个工作流做成了**一键操作**：

**1. 一键进入 Worktree**

分支列表中，所有 `worktree-` 前缀的分支都是可点击的。点击后发生三件事：
- 右侧面板的 Git / File / Search / Aux 全部**自动切换到 worktree 目录**
- 辅助终端（AuxTab）如果打开了，**自动用 worktree 路径重建**，命令也在 worktree 目录执行
- 记录「原路径」和「原分支」，分支列表中出现一个带 ← 箭头的"返回主分支"条目

不需要手动 `cd`、不需要重新打开面板——一次点击，整个 IDE 的工作上下文就切到 worktree 了。

**2. 一键返回**

点击带 ← 箭头的原分支条目，回到原始工作目录，关闭 worktree 上下文。同样是一次点击的事。

**3. 合并变更（Merge Changes）**

右键 worktree 分支，选「Merge Changes」。系统做的事情是：
- 收集 worktree 分支上的**所有变更**：已提交的 diff + 未暂存的 diff + 已暂存的 diff
- 合并成一个 patch 文件
- 用 `git apply --3way` 应用到当前分支

这意味着 Agent 在 worktree 里改的代码，可以**一条命令合并到主分支**。即使有冲突，`--3way` 策略也会尝试三方合并而不是直接报错。

**4. 冲突重试**

如果 apply 因为 index/worktree 不一致而失败，可以点「保留冲突」按钮重试——系统会先 `git add -A` 同步 index，再重新 apply。不用手动排查 `does not match index` 错误。

**5. 删除 Worktree**

右键 → Delete Branch，会自动 `git worktree remove --force` + `git branch -D`。如果你当前正在这个 worktree 里，系统**先自动返回主分支再删除**，不会让你处在一个已被删除的目录里。

**6. Session 隔离**

Worktree 导航状态是**按 Session 记录的**（`Record<string, WorktreeNav>`）。Session A 在 worktree 里干活，Session B 还在主分支——互不影响。切换 Session 时右侧面板的状态各自独立。

---

**一句话总结这个功能**：你在主分支写需求，AI Agent 在 worktree 分支改代码，改完右键合并进来——全程不用离开 IDE，不用手敲 `git worktree` 命令。

好的，以下是补充的三个部分：

---

### 十、快捷键全表

快捷键系统完全可自定义——每个快捷键都可以在设置面板中重新绑定（点击录制新按键），也可以一键恢复默认。快捷键存储在 `localStorage`，不会因为版本升级丢失。

| 快捷键        | 功能                                      | 可自定义 |
| ------------- | ----------------------------------------- | -------- |
| `Ctrl+F`      | 聚焦搜索面板（Search tab 隐藏时自动显示） | 是       |
| `Ctrl+↑`      | 上一个终端 Session                        | 是       |
| `Ctrl+↓`      | 下一个终端 Session                        | 是       |
| `Ctrl+←`      | 右侧面板标签页左移                        | 是       |
| `Ctrl+→`      | 右侧面板标签页右移                        | 是       |
| `Ctrl+H`      | 弹出命令历史浮窗（全键盘操作）            | 是       |
| `Ctrl+=`      | 字体放大（终端/编辑器各自独立）           | 是       |
| `Ctrl+-`      | 字体缩小                                  | 是       |
| `Shift+Enter` | 终端内换行但不提交                        | 是       |
| `PageDown`    | 有滚动条时翻页，无滚动条时透传 Shell      | 是       |
| `PageUp`      | 同上                                      | 是       |
| `Escape`      | 关闭 Diff 视图 / 关闭历史浮窗             | 只读     |
| `Ctrl+S`      | Diff/Edit 视图中保存文件                  | 固定     |
| `Ctrl+Enter`  | 在 commit 输入框中提交                    | 固定     |

**切 Session 和切标签页后自动聚焦**：`Ctrl+↑↓` 切 Session 后自动聚焦新终端；`Ctrl+←→` 切标签页后自动聚焦对应内容（Search 聚焦输入框，其他聚焦容器）。全都用 `focus({ preventScroll: true })`，避免浏览器滚动干扰。

**字体缩放为什么放到主进程拦截？** 因为 Chromium 会吃掉 `Ctrl+=` / `Ctrl+-` 做页面缩放。Vibe IDE 在主进程 `before-input-event` 中拦截这两个键，阻止默认缩放，通过 IPC 转发到渲染进程修改终端/编辑器字体大小。终端和编辑器各自独立记忆字体大小——在终端按 `Ctrl+=` 只放大终端，切到 Diff 视图后再按只放大编辑器。

---

### 十一、Session 右键菜单

**右键 Session 列表项**，弹出三个操作：

- **克隆（Clone）**：在右键的 Session **下方紧挨着**创建新终端，使用相同的 cwd 和 Shell 类型。克隆的会话自动获得一个独立的新 PTY 进程。

- **重命名（Rename）**：进入内联编辑模式，输入框自动聚焦并全选，Enter 确认 / Escape 取消。**手动重命名后，OSC 标题自动改名永久停用**——你再也不会看到自己改好的名字被终端覆盖掉。双击 Session 也可触发重命名。

- **关闭（Close）**：关闭终端，同时清理该 Session 的命令历史、Agent 状态、右侧辅助终端，不残留数据。

**右键空白区域**，弹出：

- **新建终端**：打开目录选择器，选完后自动存到最近目录列表。

- **最近目录（最多 10 个）**：每条记录显示文件夹图标 + 完整路径，点击一键在该目录创建新终端。目录列表在创建 Session 时自动记录，去重 + 去规范化（反斜杠统一转正斜杠），最近使用的排最前。

---

### 十二、Settings 菜单全选项

点击左侧面板标题栏的齿轮图标弹出，所有选项即时生效：

| 设置项                 | 说明                                                                                                       |
| ---------------------- | ---------------------------------------------------------------------------------------------------------- |
| **中 / EN**            | 界面语言切换，即时生效无需重启                                                                             |
| **v{版本号}**          | 当前版本，纯展示                                                                                           |
| **Theme**              | 14 套配色方案，hover 弹出侧边栏预览，每套带有主题色圆点                                                    |
| **Emoji Text**         | 自定义 Session 图标列表，一行一个 Emoji，会实时预览。支持重置默认                                          |
| **Shell Type**         | 新建终端默认 Shell，只显示本机已安装的（pwsh/powershell/cmd/git-bash/wsl），选中的有主题色标记             |
| **Keyboard Shortcuts** | 打开快捷键设置面板，可录制/重置所有绑定                                                                    |
| **File Tree Depth**    | 文件树展开深度 1-8 级，`<` `>` 按钮调节，即时生效                                                          |
| **File Filter Rules**  | 文件树和 Git watcher 跳过的目录名，一行一个，支持重置默认                                                  |
| **Word Wrap**          | 编辑器/Diff 视图是否自动换行，checkbox 切换                                                                |
| **Auto UTF-8**         | 新建终端时是否自动设置 `[console]::OutputEncoding = [Text.UTF8Encoding]::new()`（PowerShell 中文乱码预防） |
| **Show squiggles**     | Monaco 编辑器的 TS/JS 语法/语义诊断波浪线开关。关闭可减少大文件的 CPU 开销                                 |

**14 套主题**：Vibe Dark、VS Code Dark、One Dark、Dracula、Nord、Solarized Dark/Light、Monokai、Monokai Pro、GitHub Light、Tokyo Night、Catppuccin、Hatsune Miku、Hatsune Light。

**Shell 自动检测**：会自动检测 `C:\Program Files\PowerShell\7\pwsh.exe`、系统 Powershell 5、CMD、Git Bash、WSL。选中的 Shell 不存在时自动回退到下一个可用选项。

---

### 十三、终端内快捷键细节

这些没有列在快捷键设置面板里，但每天都会用到：

| 操作             | 行为                                                                |
| ---------------- | ------------------------------------------------------------------- |
| **右键**         | 有选中文本 → 复制；无选中文本 → 粘贴                                |
| **Ctrl+C**       | 有选中文本 → 复制；无选中文本 → 透传给终端（即正常的 SIGINT）       |
| **Shift+Enter**  | 发送换行但不提交（`\x1b\r`），在 Claude Code 等 REPL 中用于多行输入 |
| **选中文件路径** | 选中并松开鼠标，路径自动跳转打开                                    |

**右键复制/粘贴的判断逻辑**：先检查是否有选区——有则复制到剪贴板，无则从剪贴板读取并粘贴到终端。一个右键覆盖两种高频操作，不用先点一下取消选区再右键粘贴。
好的，以下是构建与分发的补充部分：

---

### 十四、构建与分发

#### 一键构建

```bash
npm run build:win
```

这条命令做了五件事：

1. **`npm run build`** — `electron-vite build` 编译主进程 / preload / 渲染进程三层代码到 `out/`
2. **`electron-builder --win --x64`** — 打包为 `dist/win-unpacked/` 目录（`dir` 模式，不做 installer），包含 Electron 运行时 + 编译产物 + `node-pty` 原生模块
3. **复制 `.bat` 文件** — 把 `build/` 下的 `register-context-menu.bat` 和 `unregister-context-menu.bat` 拷贝到 `dist/win-unpacked/`
4. **`scripts/set-icon.js`** — 用 `resedit` 给 exe 嵌入自定义 ico 图标
5. **`scripts/zip-win.js`** — 用 PowerShell `Compress-Archive` 打包为 `dist/Vibe-IDE-x.x.x-win-x64.zip`

产物结构：
```
dist/
├── win-unpacked/          ← 绿色免安装，直接运行
│   ├── Vibe IDE.exe
│   ├── register-context-menu.bat
│   ├── unregister-context-menu.bat
│   └── resources/
│       └── app.asar       ← 编译后的应用代码
└── Vibe-IDE-0.3.4-win-x64.zip   ← 分发用压缩包
```

#### 开发与测试

```bash
npm run dev       # 热重载开发模式
npm test          # 运行测试（test/git.test.mjs、test/search.test.mjs）
npm start         # 预览构建产物
```

#### 版本号管理

```bash
npm version patch  # 0.3.4 → 0.3.5  修 bug
npm version minor  # 0.3.4 → 0.4.0  新功能
```

版本号会自动写入 `package.json`，`build:win` 时作为 zip 文件名。

#### 分发后的操作

分发给用户的是 `Vibe-IDE-x.x.x-win-x64.zip`，解压后的使用步骤：

**第一步：右键注册（可选但推荐）**

双击 `register-context-menu.bat`，会在 Windows 注册表写入三项右键菜单：
- **任意文件** → "Open with Vibe IDE"（以文件所在目录打开，自动定位文件）
- **任意文件夹** → "Open with Vibe IDE"
- **文件夹空白处** → "Open with Vibe IDE"

实现原理：通过 `HKCU\Software\Classes\*\shell\VibeIDE\command` 注册，执行 `"Vibe IDE.exe" "%1"`。主进程的 `parseStartupPath` 会解析传入路径，目录直接创建 Session，文件则打开父目录并自动跳转到该文件的 Diff 视图。通过 `second-instance` 事件，多次右键打开会复用同一个窗口（单实例锁），每个路径在当前窗口中创建新终端或打开新文件。

卸载右键菜单：双击 `unregister-context-menu.bat`，删除上述注册表项。

**第二步：直接运行**

`Vibe IDE.exe` 是绿色免安装的——不需要安装程序，不需要管理员权限。首次启动时：
- 主进程强制 ANGLE D3D11 GPU 加速
- Monaco Editor 的 `vs/` 文件从 `out/renderer/monaco/` 加载（`copy-monaco.js` 会把 Monaco 从 `node_modules` 拷到输出目录）
- `node-pty` 因是原生模块，在 `electron-vite` 配置中声明为 `external`，不被打包进 app.asar，由 electron-builder 按 `nodeGypRebuild: false` 保持预编译的二进制

**第三步：钉到任务栏**

运行后右键任务栏图标 → 固定到任务栏，后续可以直接从任务栏启动。

---

#### 版本更新机制

当前版本采用手动分发模式——把 zip 发给用户，解压覆盖即可。`package.json` 中已声明 `electron-updater` 相关依赖（`build.appId` 等），后续可以接入 `electron-updater` 实现自动更新。