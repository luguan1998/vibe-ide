UI设计稿,暗色主题桌面IDE三栏布局应用界面,左中右三栏,中栏为空白内容占位(不要画终端):

[左栏·会话列表]
顶部居中:三段式状态徽章(运行数/空闲数/警告数,分段间细分隔线)+ 右侧设置齿轮按钮
下方:按目录分组的圆角卡片,每组卡片头部为文件夹行(emoji + 目录名),卡片内为该目录下的会话列表项

会话列表项三态(重点区分,请设计三种状态的视觉差异):
1. 未选中·空闲
2. 未选中·运行中(有动感,如文字波浪或呼吸感)
3. 选中·空闲(有选中高亮)
4. 选中·运行中(重点设计,高亮+动感的叠加状态)
5. 警告状态

每行结构:小emoji图标 + 名称文字 + (下方可选)灰字目录路径;悬停时行尾浮现操作小图标(历史/关闭)

[右栏·多tab面板]
顶部:居中的胶囊tab栏,含4个tab(Git/Aux/Dir/Nga),激活tab有高亮
内容区:圆角卡片,Git tab展示文件变更列表 + 底部提交区(快捷操作小按钮 + 提交输入框 + 推送按钮组)


请帮我生成一张 UI 控件切图图集 (UI Atlas) / Sprite Sheet